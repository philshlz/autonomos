# site/

the public face of the repo, served via GitHub Pages pointing at /site.

- `index.html` - landing page (thesis-first: models rented, state owned)
- `animation.html` - the federation / knowledge-graph animation

the existing tuned animation from the design work drops in here as animation.html. this scaffold ships a minimal index placeholder so Pages has something to serve; replace with the real landing page when ready.

note from prior review: the kawaii mascot / pixel-font aesthetic reads as mismatched with the governance tone. the landing page should look like infrastructure people trust with real commitments, not a toy.
